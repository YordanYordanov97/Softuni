@model BrandsProductsViewModel

<div class="container">
    <div class="row">
        <div class="col-sm-2">
            <h3>Filter By</h3>
            @await Html.PartialAsync("_TeamsDropdownMenuPartial", Model.Teams,
           new ViewDataDictionary(this.Vi‌​ewData) {
              {
                  "ControllerName", $"{Model.Gender}TeamProducts"
              } } )

            <br />
            <br />
            <div>Brands </div>
            <hr />

            <form method="post" asp-controller="@Model.Gender" asp-action="SelectBrand">
                @await Html.PartialAsync("_BrandsCheckBoxPartial", Model.Brands)
            </form>
            <hr />
        </div>
        <div class="col-sm-10">
            <h3>Nba Store @Model.Gender's Gear</h3>
            <hr />
            @await Html.PartialAsync("./_BrandsSortDropdownMenuPartial.cshtml", Model)
            <hr />
            @await Html.PartialAsync("_ProductsViewPartial", Model.Products)
            <br />
            @await Html.PartialAsync("./_NavBrandsPartial.cshtml", Model)
        </div>
    </div>
</div>


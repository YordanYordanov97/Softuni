@model TeamProductsViewModel

<div class="container">
    <div class="row">
        <div class="col-sm-2">
            <h3>Filter By</h3>
            @await Html.PartialAsync("_TeamsDropdownMenuPartial", Model.Teams,
            new ViewDataDictionary(this.Vi‌​ewData) {
               {
                   "ControllerName", "LadiesTeamProducts"
               } } )
        </div>
        <div class="col-sm-10">
            <h3>
                @Model.TeamWithProducts.Name Ladies's Gear
            <img src="@Model.TeamWithProducts.LogoUrl" alt="@Model.TeamWithProducts.Name" width="100" height="100" />
        </h3>
        <hr />
        @await Html.PartialAsync("_SortDropdownMenuPartial", Model.Page)
        <hr />
        @await Html.PartialAsync("_ProductsViewPartial", Model.TeamWithProducts.Products)
        <br />
        @await Html.PartialAsync("_NavTeamDetailsPartial", Model.Page)
        </div>
    </div>
</div>

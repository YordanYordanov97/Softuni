@model  ProductBindingModel
@{
    //var teams = Model.Teams as IEnumerable<Team>;
    //IEnumerable<SelectListItem> teamsSelectList = null;
    //if (teams != null)
    //{
    //    teamsSelectList = teams.Select(t => new SelectListItem() { Text = t.Name, Value = t.Id.ToString() });
    //}
    ViewData["Title"] = "Edit";
}

<h2>Edit @Model.Title</h2>
<div class="row">
    <div class="col-md-6">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            @Html.EditorForModel()
            @*<div class="form-grop">
            <label asp-for="TeamId" class="col-sm-2"></label>
            <div class="col-sm-10">
                <select asp-for="TeamId" asp-items="teamsSelectList" class="form-control"></select>
                <span asp-validation-for="TeamId" class="text-danger"></span>
            </div>
        </div>*@
            <div class="form-group">
                <div class="col-sm-10 col-sm-push2">
                    <button type="submit" class="btn btn-warning">Edit</button>
                    <a class="btn btn-default"
                       asp-area="Admin" asp-controller="Products" asp-action="Index">
                        Back
                    </a>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

